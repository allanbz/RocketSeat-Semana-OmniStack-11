import React from 'react';

import './global.css';

import Routes from './routes';

// JSX (JavaScript XML; HTML dentro do JS)
// Componente = função que retorna HTML
// Propriedade = atributo de componente ao invés de HTML

function App() {
  return (
    <Routes/>
  );
}

export default App;
